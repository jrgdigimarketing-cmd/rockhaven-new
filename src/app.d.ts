declare namespace App {
  // Keep the starter typed at the framework boundary even though the app code is JavaScript.
  interface Locals {}
  interface PageData {}
  interface Error {}
  interface Platform {}
}

export {};
